export interface Domicilio {
    id: number,
    altura?: string | null,
    calle?: string | null,
    ciudad?: string | null,
    codigoPostal?: string | null,
    departamento?: string | null,
    pais?: string | null,
    piso?: string | null,
    provincia?: string | null
}